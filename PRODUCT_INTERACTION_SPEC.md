# DSH iOS 产品与交互说明

本文档是后续 UI 和交互迭代的产品依据。目标是构建一个以工作区为入口、以会话历史为工作内容的移动端 AI 客户端。

> 2026-08-16 术语约定（覆盖后文历史文字）：产品界面随系统语言使用 Workspace / 工作区、Session / 会话、Mode / 模式、Model / 模型、Work / 工作。代码及 DSH 协议继续使用 `workspace`、`session` 或 `conversation`。不能把不同 Workspace 下的 Session 合并成同一个扁平列表。

## 1. 核心信息架构

```text
欢迎页
├── 开始新对话
├── 最近项目
└── 打开左侧项目侧边栏

项目
├── 最近一次访问的聊天
├── 历史聊天 A
├── 历史聊天 B
└── 新建聊天
```

应用包含四个主要界面状态：

1. 欢迎页。
2. 左侧项目侧边栏。
3. 项目聊天页。
4. 当前项目的历史记录列表。

核心导航原则：

- 欢迎页是默认入口和工作台。
- 左侧侧边栏负责切换项目。
- 聊天页展示当前项目中的一条具体聊天记录。
- 历史记录入口只切换当前项目内部的聊天。
- 项目和聊天历史是两级关系，不能混成一个无层级列表。

## 2. 欢迎页

应用正常启动后，优先显示欢迎页。若产品后续决定恢复上次工作现场，可以在冷启动完成数据恢复后进入最近项目；但必须保留明确返回欢迎页的能力。

欢迎页包含：

- 简洁的欢迎文案。
- 新建对话的输入入口。
- 最近访问的项目列表。
- 打开左侧项目侧边栏的按钮或边缘滑动手势。

最近项目可以显示：

- 项目名称。
- 最近更新时间。
- 最后一条聊天摘要或简短状态。
- 运行中、等待审批或离线等必要状态。

点击最近项目后：

1. 将该项目设为当前项目。
2. 优先打开该项目上次访问的聊天记录。
3. 如果项目没有历史记录，则显示该项目的空白聊天页。

欢迎页不展示某个项目的完整聊天历史。

### 2.1 新建聊天归属

全局“新建聊天”与欢迎页“开始新对话”必须先进入 DSH 归属选择，不得静默复用上一次项目或默认选择第一个项目：

1. 从 `workspace.list` 返回的已有项目中选择一个；或
2. 输入运行当前 DSH 服务的机器上的已存在绝对目录，通过 `workspace.create(path:)` 加入为项目。

iOS 当前没有远程浏览主机目录的 RPC，因此不得用手机本地文件选择器冒充主机目录选择器。目录入口明确标注为 DSH 主机路径。选定归属后进入空白聊天草稿；只有发送第一条消息时才调用 `session.create(workspaceId:)`，取消选择不得创建空 session。

## 3. 左侧项目侧边栏

侧边栏从屏幕左侧滑出，用于浏览和切换项目。

### 3.1 打开方式

- 从屏幕左边缘向右滑动。
- 点击欢迎页或聊天页左上角的菜单按钮。
- 主体面板位移、圆角和阴影必须连续跟随手指。

侧边栏关闭时，仅从屏幕左侧约 20–24 pt 的边缘区域开始识别打开手势，避免与正文滚动或其他横向交互冲突。

### 3.2 打开状态

- 侧边栏覆盖屏幕宽度约 75%–80%。
- iPhone、iPhone 横屏与 iPad 统一使用“底层侧栏 + 上层主体”的揭示式层级：侧栏固定在下面，主体向右滑开进入可视区域。
- 主体与侧栏都是连续圆角表面；主体左缘位于侧栏上方，并向底层投下克制阴影。
- 上层主体保持原始亮度和交互能力；抽屉展开期间不显示暗色遮罩，也不放置拦截点击的透明层。
- 两层不能在滑动中互相穿插、交换 z-order 或突然贴成无层级的并排平面。
- 当前项目使用深灰色圆角背景高亮。
- 列表较长时，仅中间项目区域滚动；底部核心操作保持可用。
- 每个项目在侧边栏中默认展示最近 3 条聊天；超过 3 条时折叠，并提供明确的展开/收起动作。

### 3.3 关闭方式

- 在已打开的侧边栏上向左滑动。
- 点击工具栏中的侧边栏切换按钮。
- 点击左上角关闭或菜单按钮。
- 选择项目后自动关闭。

建议手势判定：

```text
打开手势起点：屏幕左边缘 20–24 pt 内
完成阈值：移动超过面板宽度约 35%，或具有明显完成方向速度
未达到阈值：回弹至原状态
动画时长：约 280–350 ms
```

必须支持交互式取消：用户拖动一半后反向滑动，侧边栏应跟随手指回到原位置。

## 4. 项目列表

侧边栏第一层展示项目，而不是所有聊天历史。

每个项目行可以包含：

- 项目名称。
- 可选项目图标。
- 可选更新时间。
- 可选运行中或未处理状态。
- 右侧历史记录入口或展开指示。
- 当前选中状态。

点击项目主体：

1. 切换当前项目。
2. 恢复该项目最近访问的聊天。
3. 没有历史时显示空白聊天页。
4. 自动关闭侧边栏。

点击项目行右侧的历史记录入口：

- 展示该项目下的聊天历史。
- 当前项目可同时切换为被点击项目。
- 不展示其他项目的聊天记录。

## 5. 项目历史记录

历史记录始终隶属于一个项目。数据关系必须保持为：

```text
Project
├── Conversation A
├── Conversation B
└── Conversation C
```

历史记录行可以显示：

- 对话标题。
- 最后一条消息摘要。
- 更新时间。
- 运行状态或待处理状态。
- 当前打开状态。

历史记录较多时，可以按时间分组：

```text
今天
昨天
过去 7 天
更早
```

点击历史记录后：

1. 保持所属项目为当前项目。
2. 将该记录设为当前聊天。
3. 更新聊天正文与标题。
4. 更新列表选中状态。
5. 关闭历史列表或侧边栏。
6. 保存该项目最后访问的聊天 ID。

## 6. 聊天页

聊天页展示当前项目中的一条具体聊天记录。

页面包含：

- 左上角侧边栏入口。
- 当前项目或当前聊天标题。
- 右侧历史记录及更多操作入口。
- 中部可滚动聊天内容。
- 底部固定输入区。

主要行为：

- 点击左上角打开项目侧边栏。
- 点击右侧历史记录入口，切换当前项目内的聊天。
- 选择其他项目时，加载该项目最近访问的聊天。
- 选择其他历史记录时，项目不变，只替换当前聊天内容。
- 新建聊天时，将新记录归属到当前项目。
- 点击“新建聊天”只进入本地草稿状态；在用户发送第一条有效消息前，不得调用 `session.create`，避免产生其他客户端不可见的空会话。
- 侧边栏、搜索结果和最近记录不展示协议标记为 `blank` 的历史空会话。
- 如果当前没有项目，新建聊天前必须创建项目、选择项目，或按照产品默认规则归入默认项目。

## 7. 欢迎页和聊天页的区别

欢迎页：

- 没有具体聊天正文。
- 强调开始新对话和最近项目。
- 是应用入口和工作台。

聊天页：

- 展示当前项目中的具体对话。
- 输入内容默认发送到当前项目和当前聊天。
- 提供项目切换和项目内历史切换入口。

## 8. 建议状态模型

界面至少需要维护以下概念状态；具体命名可按照现有架构调整：

```text
selectedProjectID
selectedConversationID
isSidebarPresented
isHistoryPresented
sidebarDragProgress
conversationDraftByProject
lastConversationIDByProject
```

状态规则：

- `selectedProjectID == nil` 时显示欢迎页。
- 选择项目后优先恢复对应的 `lastConversationID`。
- 切换聊天记录不能改变 `selectedProjectID`。
- 切换项目时恢复该项目上次访问的聊天。
- 新建聊天必须绑定当前项目。
- 不同项目的未发送草稿应分别保存。
- 主体位移、圆角和阴影由同一个 `sidebarDragProgress` 连续驱动。
- 数据加载、离线、失败和空状态均应有明确表现，不能以空白页面代替。

## 9. 边界情况

- 没有项目：欢迎页显示明确的创建项目入口。
- 项目没有历史：进入空白聊天页，输入框可直接开始第一条聊天。
- 当前聊天被删除：回到该项目最近一条历史；若无历史则进入空白聊天页。
- 当前项目被删除：回到欢迎页，并清空当前聊天选择。
- 加载项目失败：保留现有页面并显示可重试提示。
- 网络断开：允许浏览已缓存内容；发送区域明确显示离线状态。
- 切换项目时仍有流式输出：不得把旧项目的增量内容写入新项目界面。
- 键盘弹出时打开侧边栏：先平滑收起键盘，再执行侧边栏交互。

## 10. 交互验收标准

- 可以从屏幕左边缘右滑打开项目侧边栏。
- 可以左滑、点击工具栏切换按钮或选择项目关闭侧边栏；主体本身在展开期间仍可操作。
- 拖动过程中面板必须跟手，允许取消手势。
- 欢迎页能展示最近项目并进入项目。
- 项目选择和聊天历史选择是清晰的两级导航。
- 切换项目能恢复该项目最后访问的聊天。
- 切换历史记录不会意外改变项目。
- 当前项目和当前聊天均有明确选中状态。
- 新建聊天正确归属于当前项目。
- 页面切换后不会发生跨项目内容、草稿或流式事件串线。
