# DSH iOS UI 设计规范

本文档描述移动端界面的通用视觉语言、布局、组件和状态。参考来源包括 ChatGPT 移动端深色界面和 Codex iOS 浅色会话界面，但实现必须服务于 DSH iOS 自身的信息架构，不能机械复制截图像素。

会话页的顶部栏、消息、运行状态、输入器、模型面板和更多菜单以 `CODEX_UI_REFERENCE.md` 为优先细节规范；侧边栏和设置页继续参考本文及 `PRODUCT_INTERACTION_SPEC.md`。所有界面必须使用语义色，同时支持浅色与深色模式。

所有截图都只是设计参考。任何新增控件或状态必须先映射到 DSH 当前 `AppModel`、`SessionModel`、事件或 RPC；没有真实数据来源的 Speed、Pin、运行时长、文件汇总等不得进入实现。

全局新建聊天使用原生 sheet：先显示已有项目列表（项目名 + 主机路径），末尾提供“使用机器上的目录”。目录页只接受当前 DSH 主机上的绝对路径并调用真实 `workspace.create`；不得展示并不存在的远程文件浏览器。选定项目前不进入聊天，也不创建 session。

截图原始画布约为 945 × 2048 px，并可能使用较大的系统显示比例。本文尺寸统一使用 iOS 逻辑尺寸 pt；不要直接把截图像素作为实现尺寸。

## 1. 设计方向

界面采用极简、内容优先、原生系统材质的设计：

- 深色模式页面背景接近纯黑；浅色模式使用系统白色/背景色。
- 使用语义表面和系统材质表达层级，避免依赖明显阴影。
- 使用较大的圆角、宽松留白和统一的线性图标。
- 主操作根据上下文使用高亮蓝色或高对比黑/白，其余内容保持语义中性色。
- 聊天正文是视觉主体，导航和装饰退居次要层级。
- 中文和英文均使用系统无衬线字体。
- 不使用大面积渐变、霓虹发光、厚重描边或高强度投影。

关键词：`内容优先 / 语义色 / 系统材质 / 克制 / 柔和圆角 / 系统级`

## 2. 基础设计 Token

### 2.1 颜色

使用语义色，业务组件不要散落硬编码色值：

```text
backgroundPrimary      #000000
backgroundSecondary    #1C1C1E
surfacePrimary         #202022
surfaceElevated        #29292B
surfaceSelected        #242426

textPrimary            rgba(255,255,255,0.96)
textSecondary          rgba(255,255,255,0.60)
textTertiary           rgba(255,255,255,0.38)
textDisabled           rgba(255,255,255,0.25)

iconPrimary            rgba(255,255,255,0.96)
iconSecondary          rgba(255,255,255,0.58)

separator              rgba(255,255,255,0.10)
borderSubtle           rgba(255,255,255,0.14)
overlayScrim           rgba(0,0,0,0.62)

accentBlue             #0A84FF
accentGreen            #20C997
warning                #FF9F0A
destructive            #FF453A
```

规则：

- 主页面使用纯黑或接近纯黑背景。
- 设置卡片和浮动控件比页面背景亮一个层级。
- 选中状态只需要稍亮的深灰底，不使用蓝色整行背景。
- 蓝色仅用于核心 CTA、语音按钮、焦点和升级入口。
- 普通描边必须非常弱。

### 2.2 圆角

```text
radiusSmall       10
radiusMedium      16
radiusLarge       24
radiusXLarge      30
radiusSheet       36–42
radiusPill        完全胶囊形
```

- 列表组卡片：24–28。
- 输入框：完全胶囊形。
- 圆形图标按钮：正圆。
- 侧边栏和大型浮层：外侧圆角 32–40。
- 不要给每个小元素都增加独立卡片。

### 2.3 间距

使用 4 pt 基础网格：

```text
4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48
```

推荐值：

- 页面左右边距：20–24。
- 图标与文字：14–16。
- 章节之间：28–36。
- 标题与列表组：12–16。
- 列表行左右内边距：20。
- 标准列表行高：58–64。
- 正文段落之间：22–28。

### 2.4 字体

使用系统字体和语义字号：

```text
页面标题          28–30 / Semibold
个人名称          20–22 / Semibold
章节标题          17–19 / Semibold
列表标题          17–19 / Regular
正文              17–19 / Regular
次要值            16–18 / Regular
按钮文字          17–19 / Semibold
元数据            14–16 / Regular
```

- 中文正文行高约为字号的 1.55–1.7 倍。
- 页面和章节标题使用 Semibold，不使用 Black。
- 次要信息通过语义色降低层级，不要缩小到难以阅读。
- 所有核心文本支持 Dynamic Type。

## 3. 图标

- 使用统一的圆角线性图标，优先 SF Symbols。
- 标准视觉尺寸 22–24。
- 图标放在 28–32 宽的固定布局区域中，保证文字对齐。
- 普通图标使用主白色，弱操作使用次级灰色。
- 不混用粗线、细线、实心和彩色图标。
- 每个图标按钮的实际触控区域至少为 44 × 44。

## 4. 欢迎页

欢迎页是默认工作台，应比聊天页更轻、更留白。

布局建议：

1. 顶部安全区下方放置菜单入口和必要的账户入口。
2. 页面上半部显示欢迎文案和简短说明。
3. 中部或下半部显示最近项目。
4. 底部放置新建对话输入入口或核心 CTA。

最近项目卡片：

- 使用深灰表面，圆角 20–24。
- 卡片之间间隔 10–12。
- 项目名称使用 17–19 Semibold。
- 摘要和更新时间使用次级文字。
- 状态信息保持小而清晰，不大面积使用状态色。
- 整张卡片可点击，触控反馈为短暂亮度变化或轻微缩放。

空状态应显示一句解释和一个明确操作，不使用装饰性大插画占据主体。

## 5. 左侧项目侧边栏

### 5.1 容器

- 从左侧滑出，宽度为屏幕的 75%–80%。
- 背景使用 `backgroundPrimary`。
- 列表直接排版在页面背景上；禁止再把搜索、添加项目或每个工作区包成大块白色卡片。
- 浅色模式使用系统白色背景，不使用紫灰色分组底。
- 右侧边缘使用 32–40 的大圆角。
- iPhone、iPhone 横屏与 iPad 的抽屉统一采用固定底层侧栏和可移动上层主体：主体的背景层和真实内容层必须使用同一个连续圆角裁切，圆角随打开进度从 0 平滑增长。iPhone 约 44pt，iPad 约 28pt，以贴合对应设备的屏幕轮廓；左缘向侧栏投下柔和阴影。
- 侧栏宽度以 320pt 为基准，使横屏与 iPad 上的主体仍保留足够可见面积。
- 主体的视觉表面（背景、圆角和阴影）必须延伸至物理屏幕上下边缘，拖开后不允许在状态栏或 Home 指示条区域出现几何空白。
- 导航、正文、Composer 和侧栏底部操作属于独立安全内容层，继续遵守系统安全区；不得让内容层整体忽略安全区。
- 侧栏的右上、右下角约 30；连接屏幕左边缘的一侧不额外露出圆角缺口。
- 抽屉展开不是模态状态：禁止给主体增加暗色遮罩、降低亮度或放置透明点击拦截层。主体与侧栏必须同时保持正常亮度和完整交互。
- 投影只用于表达上层主体压住底层侧栏的空间关系，不能变成四周均匀的厚重光晕。

### 5.2 顶部区域

- 左侧显示产品标题，28 左右，Semibold。
- 右侧使用 48–52 的圆形搜索或关闭按钮。
- 按钮背景使用深灰表面和极弱描边。
- 顶部左右边距约 24，并避开系统安全区。

### 5.3 项目行

- 行高 52–60。
- 左侧图标区域宽 28–32。
- 项目名称单行显示，过长时尾部省略。
- 右侧可放状态、历史入口或 disclosure indicator。
- 未选中项目背景透明。
- 当前项目使用 `surfaceSelected` 圆角背景，圆角 14–16。
- 选中状态不使用蓝色竖条或蓝色整行填充。
- 项目与聊天均使用扁平行；只允许当前聊天出现弱圆角选中底，不为普通行增加分隔卡片和外框。
- 搜索默认收起，由顶部搜索按钮按需展开为一条紧凑输入框。
- 每个项目默认只露出最近 3 个聊天；更多记录通过项目内纯三点按钮展开或收起，界面不得显示“展开其余 N 个聊天”之类的长文案。完整状态说明仅通过辅助功能标签提供。
- 展开/收起三点与 session 标题使用相同的 10pt 行内左边距，不设置额外层级缩进；按钮命中区域仍覆盖整行宽度。
- 项目标题可以保留文件夹图标；项目下的 session 行只显示单行标题，前面不放气泡、Logo 或其他装饰图标。

列表可以按“置顶”“最近”等章节分组。章节标题使用 17–18 Semibold，章节之间至少间隔 24。

### 5.4 底部操作

底部可以悬浮：

- 左侧蓝色“新建聊天”胶囊按钮。
- 右侧深灰色设置圆形按钮。

新建聊天按钮高度约 56，使用白色 Semibold 文字和编辑图标。设置按钮约 56 × 56。项目列表滚动区域必须为底部控件预留 inset。

底部操作区不使用独立灰色、模糊或材质横条；按钮直接浮在与侧栏一致的页面背景上。

## 6. 历史记录列表

历史记录可以作为侧边栏第二级内容、独立浮层或右侧弹出层呈现，但同一版本中必须保持一致。

历史行包含：

- 对话标题。
- 一行摘要或时间。
- 可选运行状态。

视觉规则：

- 行高 54–68，依据信息数量调整。
- 标题使用主文字；摘要和时间使用次级文字。
- 当前聊天使用深灰圆角背景。
- 分组标题保持弱层级，不与聊天标题争夺注意力。
- 删除、重命名等次要操作放入上下文菜单，不常驻占用行宽。

## 7. 聊天主页面

### 7.1 顶部导航

顶部不使用厚重的整条导航栏，而使用轻量浮动控件：

- 左侧为 48–52 的圆形菜单按钮。
- 中间标题按空间显示当前项目或聊天标题，过长时省略。
- 右侧为历史记录及更多操作入口，可采用单个圆形按钮或双图标胶囊。
- 控件背景使用半透明深灰表面和极弱描边。
- 不添加明显投影。

### 7.2 消息正文

- 页面左右边距约 20。
- 正文字号 17–19，行距宽松。
- 助手长回答优先直接排在黑色背景上，不强制放入聊天气泡。
- 段落之间保留约 24 的垂直间距。
- Markdown 标题、列表、代码、引用和表格必须有明确层级。
- 引用可使用左侧短竖线和轻微缩进。
- 工具调用、审批、提问和统计可以使用低对比度深灰卡片。
- 来源标签使用小型胶囊，展示 favicon 和简短域名。

用户消息如需气泡，保持紧凑、深灰、右对齐，不使用高饱和蓝色大气泡。

### 7.3 回答操作区

回答结束后可以显示复制、朗读、反馈、分享、更多和来源入口。

- 图标横向排列，间距 18–22。
- 普通状态不带独立按钮背景。
- 图标使用次级白色，触控区仍需达到 44 × 44。
- Sources 可以使用重叠 favicon 加文字。

### 7.4 输入框

- 固定在页面底部和键盘上方。
- 页面左右边距约 14。
- 最小高度 56–60，多行输入时向上增长。
- 使用深灰胶囊背景和极弱描边。
- 左侧为附件或加号按钮。
- 中间为输入文本或三级灰 placeholder。
- 右侧为发送、麦克风或语音模式按钮。
- 核心语音或发送动作可以使用 44–48 的蓝色实心圆。
- 获得焦点时只显示系统焦点和蓝色光标，不给整个输入框增加强蓝描边。

键盘出现时，输入框与键盘同步移动，正文滚动区域必须增加底部 inset。

## 8. 设置页

设置页可作为全屏 sheet 展示：

- 容器背景使用 `backgroundSecondary`。
- 顶部左右圆角 36–42。
- 右上角放置 48–54 的圆形关闭按钮。
- 页面内部纵向滚动。

用户信息区居中：

- 头像 76–88。
- 头像右下角叠加 32–36 的编辑按钮。
- 用户名位于头像下方 12–16，使用 20–22 Semibold。

设置章节由“章节标题 + 分组卡片”构成：

- 分组卡片背景使用 `surfaceElevated`。
- 圆角 24–28。
- 行高 58–64。
- 分隔线从文字起点开始，不穿过左侧图标区。
- 可导航行显示弱灰 chevron。
- 只读信息显示右侧值，不显示 chevron。
- 即时动作不显示值和 chevron。
- 强调入口仅将图标和标题改为蓝色，不增加蓝色整行背景。

## 9. 动效

- 侧边栏开关：280–350 ms，交互式水平位移，系统弹簧或 ease-out。
- 页面或历史切换：200–300 ms，以轻微位移加淡入淡出表达层级。
- 设置 sheet：300–400 ms，从下方或当前层级平滑进入。
- 按钮按下：缩放至 0.96–0.98，并轻微降低亮度。
- 列表行点击：短暂浅灰高亮。
- 避免夸张弹跳、视差、发光和长时间渐变动画。
- 开启 Reduce Motion 时用淡入淡出替代大幅位移。

## 10. 无障碍和适配

- 所有触控区域至少 44 × 44。
- 核心文本支持 Dynamic Type。
- 不能只依赖颜色表示状态。
- 所有纯图标按钮提供 accessibility label。
- 键盘弹出时输入框和当前内容不能被遮挡。
- 侧边栏打开后，右侧主体继续正常响应按钮、滚动、输入和导航操作；关闭侧栏使用工具栏按钮或横向收起手势。
- 使用系统安全区适配刘海、灵动岛和 Home Indicator。
- 中英文切换后不使用固定文字宽度。
- 长项目名、长聊天标题、邮箱和 URL 不得重叠或溢出。
- 演示数据不得显示真实邮箱、电话号码或其他敏感信息。

## 11. 禁止的视觉偏差

不要做成：

- 每个元素都有描边的“卡片海”。
- 大面积蓝紫渐变或霓虹效果。
- 高强度阴影和过度玻璃拟态。
- 过小正文或过密列表。
- 每一行都显示 disclosure indicator。
- 多套风格不一致的图标混用。
- 助手长回答全部塞入聊天气泡。
- 传统厚重导航栏挤压正文空间。
- 用中灰色替代主页面的纯黑背景。
- 生硬照搬参考截图像素。

## 12. UI 验收标准

- 页面以黑色和深灰为主，蓝色只用于关键动作。
- 欢迎页、侧边栏、历史列表和聊天页的层级清楚。
- 浮动按钮、输入框和卡片遵循统一圆角语言。
- 图标使用同一套线性风格。
- 侧边栏打开时，底层内容仍可感知但明显退后。
- 当前项目和当前聊天具有清晰但克制的选中状态。
- 聊天正文具有最高视觉优先级。
- 输入框始终位于键盘上方。
- 长文本和放大字体不会造成重叠或关键功能不可用。
- 整体观感安静、直接、系统化，不带模板化装饰。

## 13. DSH Web 对齐补充（2026-08-16，优先级最高）

本节覆盖本文中仍使用“项目”、旧欢迎页或朗读动作的历史描述。

- 产品术语统一为 Workspace / Session / Mode / Model / Work；中文依次为工作区 / 会话 / 模式 / 模型 / 工作。界面字符串跟随系统语言。
- 侧边栏品牌采用 DSH 官方鲸鱼标记、`deepseek` 字标与 `HARNESS` 徽标。点击品牌进入欢迎页，不承担关闭按钮职责。
- Workspace 标题同行右侧放过滤与添加工作区按钮。Workspace 行高 34pt；Session 行高 32pt，Session 行不加前置聊天 logo。
- Workspace 使用 Web 同源 closed/open folder 图标；展开且为当前工作区时使用业务蓝色。每个工作区默认最多显示 3 个 Session，多余内容以与 Session 左边缘对齐的 `…` 展开。
- 未选择 Session 时，主体默认显示官方鲸鱼 + `Into the Unknown` 欢迎页；点击侧边栏品牌也回到该页。欢迎 Composer 上方只显示后端真实存在的 Workspace 与 Agent Preset 选择，并相对输入框左右各额外内收 14pt；不得伪造模式、模型或本地目录能力。
- 欢迎页的 Workspace、Agent Preset、Model、Reasoning Effort 与 Access 都是新会话草稿字段。未选择 Workspace 时只禁用发送，不得禁用或淡化其他控件；这些选择本身不得创建空 Session。发送首条消息时依次应用 Access、创建 Session、应用 Model/Reasoning，再发送内容。
- 切换 DSH Host 时必须清空并完整重载全部 host-scoped 数据：Sessions、Workspaces、Agent Presets、Providers、Credentials、Models、Plugins 与 Permission。禁止把上一主机的缓存误显示为当前主机能力。
- 欢迎页鲸鱼必须独立成行，采用约 62 × 48pt 的官方标记；`Into the Unknown` 位于下一行，不能把图标压缩成标题内的小装饰。
- Composer 外框采用 22pt 连续圆角，不做全高胶囊。Access 使用 DSH Web 的 Read Only / Workspace Write / Full Access 三套官方盾牌 SVG，模式使用官方 Agent Preset SVG。主体左上侧边栏入口属于系统导航，使用 SF Symbols `folder`。
- 模型选择严格读取每个模型可选的 `reasoning` 元数据。模型没有 `reasoning` 时不显示思考强度；存在 `reasoning` 时才显示 Host 提供的强度名称、说明与 `defaultEffort`。若 Host 没有指定默认强度，则提供“提供商默认”，提交时省略 `reasoningEffort`。
- 抽屉展开时主体保持正常亮度和可交互。主体是物理屏幕全高的连续圆角表面；键盘只调整内容/Composer，不能把主体外轮廓向上推。
- 聊天右上 `ⓘ` 与 `…` 使用系统 toolbar 安全区。更多菜单只有模式、模型、工作；模式和模型分别打开选择 sheet，工作沿用工作 sheet。
- Assistant 消息尾部动作只显示图标。保留复制、赞、踩；删除朗读，改为 DSH Web 的“在新对话中分支”，且只在已完成轮次的最后一条 Assistant 消息上可用。
- 消息正文长按只进入系统文本选择，不为整条消息添加 context-menu preview、缩放或悬浮快照。编辑、重新生成、删除等低频操作放在消息尾部 `…` 菜单。
- 思考与相邻工具调用合并成一条平面活动摘要，默认收起。摘要按真实调用概括为读取、搜索、编辑、命令等；展开后每个思考/工具事件占单独 30pt 行，具体输出再由该行展开。禁止恢复成多层大卡片。
- DSH 业务图标优先复用 Web SVG；侧边栏、系统导航、照片、麦克风等重大系统能力可使用 Apple SF Symbols。

## 14. Settings 与 DSH Web 对齐

- 设置导航顺序固定为 General → Models → Agent presets → Plugins；iOS 特有的 Connection & Pairing 置于最前并独立保留。
- General 只呈现 Web 已有的 Appearance（Light / Dark / System）与 Default Agent preset。默认预设变更调用真实 `settings.update`，只影响后续新会话。
- Models 将真实 Provider、API Key 状态和模型目录放在同一页。没有后端字段或 RPC 的 Provider 参数不得伪造。
- Agent presets 按 Built-in / Custom 分组，显示当前默认状态；Plugins 使用真实 inventory 状态与搜索，术语跟随 Web。
- iPhone 使用系统导航推进各页；宽屏设备使用侧栏与内容并排。分组采用平面列表和克制卡片，不复制桌面 Web 的固定像素宽度。
- iPhone 打开设置必须先显示五项设置目录，不得因默认选中 General 而直接折叠进详情；详情页使用系统返回按钮回到目录。iPad 保留弹窗内双栏，并默认展示 General。
- 内置 Agent preset 依照 Web 端用稳定 id 本地化，不能直接照搬 Host 的可变名称；`code` 在英文显示 `Code mode`，中文显示 `PTC 编程模式`，以同时保留 Web 的 PTC 术语和明确的编程语义。自定义 preset 仍显示 Host 提供的名称。

## 15. Trajectory 抽屉

- 入口位于会话页右上 `…`，术语固定为 Trajectory / 轨迹；数据直接来自 `session.history` 的原始 `type / seq / time / data` 事件与 host tool view，不增加虚构 RPC。
- 轨迹以从下向上的系统 sheet 呈现，支持 medium / large detent；抽屉内用 Turn 分段，以 User、Assistant、Tool、Subtool、Context、Compaction、Error 等真实类别显示。
- 顶部保留 Web 的 Duration、Turns、Calls、Search 语义：Duration 在序号与记录耗时之间切换，并驱动时间条宽度；Turns 全部折叠/展开；Calls 折叠/展开工具调用；Search 对事件类型、标题、预览和原始 data 做多词匹配。
- iPhone 紧凑宽度下 Duration / Turns / Calls 只显示对应图标并保留完整 accessibility label，避免英文术语被挤成多行；iPad/宽屏可显示图标与文字。
- 点击记录进入详情，展示 Event、Sequence、Turn、Step、Duration、Time、Preview、原始 Event data，以及存在时的 Host view。
- 历史分页使用 `session.history(beforeSeq:maxMessages:)`，不得把“当前页没有更早记录”误写成会话完整历史。
