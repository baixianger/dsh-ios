# Codex iOS 页面细节参考

本文把 2026-08-16 提供的 8 张 Codex/ChatGPT iOS 截图转换成可执行的文字规范，供无法读取图片的 agent 使用。它是 DSH iOS 会话页细节的优先视觉参考。

## 0. 硬约束：能力驱动，禁止无中生有

截图只提供视觉和交互方向，不代表 DSH 已拥有相同产品能力。任何控件、状态、数字和菜单项都必须能映射到当前客户端模型或后端事件：

- 有真实状态或 RPC 才显示；没有数据来源就隐藏，不放禁用占位，也不伪造演示数据。
- 模型、推理强度、权限、目标进度和子代理数量只能使用后端返回值。
- 不根据等待时间猜测任务阶段，不虚构运行时长、文件数、diff 行数或进度百分比。
- 新增 UI 前先在 `AppModel`、`SessionModel`、`Models` 和 RPC 客户端中指出数据来源。
- 如果截图能力尚未存在，应记录为候选需求，不得写成当前实现验收项。

## 1. 参考优先级

- `PRODUCT_INTERACTION_SPEC.md` 决定产品结构：欢迎页、项目侧边栏、项目内聊天历史。
- 本文决定会话页的顶部栏、正文、运行状态、输入器、模型面板和更多菜单。
- `UI_DESIGN_SPEC.md` 提供通用 token、无障碍和深色模式规则。
- 新 Codex 截图与旧 ChatGPT 截图冲突时，会话页细节优先采用本文；旧截图仍用于侧边栏和设置页的信息组织。
- 参考的是层级、密度和交互，不复制 Codex 品牌名称、数据或专有图标。

## 2. 已保存的截图

所有图片均为 590 × 1280 px，仅用于视觉比对，不能直接把像素当作 iOS pt：

- `docs/ui-reference/codex/01-chat-composer.jpg`：会话顶部、用户消息、目标状态和输入器。
- `docs/ui-reference/codex/02-model-picker.jpg`：模型与推理强度的快速选择层。
- `docs/ui-reference/codex/03-advanced-model-sheet.jpg`：高级模型配置 sheet。
- `docs/ui-reference/codex/04-running-state.jpg`：子代理、进度、目标和运行中输入器。
- `docs/ui-reference/codex/05-overflow-menu.jpg`：会话更多菜单。
- `docs/ui-reference/codex/06-stream-activity.jpg`：stream 中的 reasoning、工具活动、steer 消息和紧凑 Composer。
- `docs/ui-reference/codex/07-sidebar-light-reference.jpg`：浅色扁平侧边栏目标参考。
- `docs/ui-reference/codex/08-sidebar-before-redesign.jpg`：2026-08-16 重做前的 DSH 侧边栏差距记录。

## 3. 总体视觉语言

默认参考状态是浅色界面：

- 内容背景为接近系统白色的纯净平面。
- 正文直接排版在背景上，不套大卡片。
- 浮动导航、输入器、状态胶囊和菜单使用白色或系统材质。
- 材质层只用于交互控件和浮层，不覆盖普通正文。
- 主要文本和核心动作使用接近纯黑；说明和状态使用系统次级灰。
- 破坏性动作使用系统红色。
- 圆角较大，边界主要依靠材质、极细描边和柔和阴影表达。

实现要求：

- 必须使用语义色并同时支持深色模式。
- iOS 17–25 使用 `.thinMaterial`、`.regularMaterial` 等系统材质实现层级。
- 如果部署环境支持更新的系统玻璃 API，可渐进增强，但不能让旧系统失去可读性。
- 不手工绘制高亮反射、强模糊或发光边缘来伪造玻璃。

## 4. 会话顶部栏

顶部栏由三个独立视觉区域组成，而不是一整块厚重导航栏。

### 4.1 左侧返回/侧边栏按钮

- 独立圆形材质按钮。
- 视觉直径约 52–60 pt，实际触控区不得小于 44 × 44 pt。
- 图标使用粗细适中的 `chevron.left` 或侧边栏符号。
- 按钮与屏幕左侧保持约 16–20 pt 间距。

在 DSH iOS 中，如果当前导航行为是打开项目侧边栏，可以保留侧边栏图标；不要为了复制截图把语义错误地改成返回。

### 4.2 中间标题

- 第一行显示会话标题，使用 headline/semibold，单行尾部省略。
- 第二行显示项目或主机上下文，例如“项目名 · 主机名”。
- 副标题使用 caption/subheadline 和 secondary 颜色。
- 标题区域左对齐，不使用居中大标题。

### 4.3 右侧操作胶囊

- 一个横向材质胶囊，容纳两个独立按钮。
- 左侧为新建/编辑会话，右侧为更多菜单。
- 高度约 50–58 pt，每个图标拥有独立 44 pt 触控区。
- 图标之间有明显留白，不增加竖分隔线。

顶部栏允许内容从其下方滚动经过，但必须通过材质或安全区保证标题始终可读。

## 5. 消息与正文

### 5.1 助手内容

- 助手回答直接显示在页面背景上。
- 中文正文使用系统 body，推荐 17–19 pt 等效语义字号。
- 行高宽松，段落之间保持 16–24 pt。
- Markdown、代码差异和文件卡片保持结构清晰，但不要把整段回答包进气泡。

### 5.2 用户消息

- 用户消息右对齐。
- 使用高对比黑色圆角气泡，浅色模式下文字为白色。
- 气泡宽度不超过可用宽度约 72%–78%。
- 圆角约 22–28 pt，水平内边距 16–20 pt，垂直内边距 12–16 pt。
- 深色模式使用与背景可区分的 elevated surface，不能出现黑色融入黑色背景。

### 5.3 消息操作

- 复制、赞、踩、展开等图标直接横排在内容下方。
- 默认不为每个操作增加独立圆形背景。
- 图标使用 secondary 灰，但触控区仍至少 44 × 44 pt。
- 只有消息模型能确认该消息确实被发送为目标时，才可显示“Sent as goal”等弱说明；当前 DSH 没有该逐消息元数据，因此不显示。

## 6. Stream、思考、工具和子代理活动

运行过程应压缩成弱层级活动行，并严格由真实事件驱动：

- `turn/start` / `turn/end` 驱动 `SessionModel.isRunning`，只在运行时显示停止动作和轻量进度反馈。
- `assistant/chunk` 的 `text-delta` 直接更新助手正文；流式阶段可用纯文本优先保证刷新性能，结束后恢复 Markdown 展示。
- `reasoning-delta` 与消息中的 reasoning 进入统一“思考过程”内容，不额外制造一条假的助手回复。
- `tool/call` / `tool/result` 生成真实工具活动；标题、参数、结果和状态都来自事件。
- reasoning 与相邻的连续工具调用由 `TranscriptBuilder` 合并为一个 `ActivityGroupCard`，不要分别堆叠多个折叠卡。
- 折叠标题根据真实内容生成：“思考过程”、“工具调用 · N”或“思考过程 + 工具调用 · N”。
- 展开后按发生顺序展示；读取和 diff 使用已有 `ReadCardView` / `DiffCardView`，其余工具使用通用行。
- 活动块默认折叠，完成后仍可回看；流式期间使用细圆环或轻量状态，不使用占满正文的大 spinner。
- 只有 `SessionModel.subagents` 有条目时才显示子代理行与真实数量、状态；无条目时整行隐藏。
- 截图中的“Steering conversation”不是当前 DSH 暴露的持久状态。steer 内容仍作为真实用户消息显示，但不能伪造该阶段标签。
- 截图中的文件总数和 `+/-` 汇总当前没有会话级数据源。保留已有单次 diff 卡，不显示假的聚合胶囊。

## 7. 输入器

输入器固定在键盘或底部安全区上方，是页面最主要的交互表面。

### 7.1 容器

- 使用大圆角矩形材质，左右边距约 16–20 pt。
- 单行状态最小高度约 104–118 pt；多行文本向上增长。
- 圆角约 30–36 pt。
- 使用极细浅色描边和柔和环境阴影，不能有明显黑边。
- 键盘出现时与键盘同步移动。

### 7.2 文本区域

- Placeholder 为“Follow up”或业务对应中文文案。
- 文字从容器左上开始，水平内边距约 20–24 pt。
- Placeholder 使用 tertiary 灰。
- 输入多行时操作栏固定在容器底部，不与文字重叠。

### 7.3 底部操作栏

从左到右建议为：

1. 附件“+”。
2. 权限或安全状态。
3. 可选运行 spinner。
4. 模型图标和“模型名 + 推理强度”。
5. 麦克风。
6. 发送或停止按钮。

规则：

- 普通操作使用黑色/primary 图标。
- 权限风险可使用系统橙色或红色小图标，不能大面积染色。
- 模型名称使用 semibold；推理强度使用 secondary 色。
- 发送/停止是最强动作，使用约 48–54 pt 的黑色实心圆。
- 运行时圆内显示白色停止方块；可发送时显示发送符号。
- 每个操作的点击区域不得因视觉紧凑而小于 44 pt。

## 8. 输入器上方状态胶囊

运行任务时，可在输入器上方横向显示一组紧凑状态胶囊。只显示有真实来源的项目：

- 目标状态：来自 `SessionModel.goal.phase`，例如“正在推进目标”；`GoalState` 当前没有可靠开始时间，因此不显示“33s”。
- 进度状态：仅在 `maxGoalRounds > 0` 时显示真实的 `roundsStarted / maxGoalRounds`。
- 子代理状态：仅在 `subagents` 非空时显示真实数量。

视觉规则：

- 高度约 36–42 pt。
- 白色/材质背景，完全胶囊圆角。
- 图标、文字和数字使用 secondary 到 primary 的层级变化。
- 多个胶囊之间保持 8–10 pt。
- 空间不足时允许水平滚动或优先隐藏低价值胶囊，禁止压缩文字到不可读。
- 点击胶囊打开对应详情，例如目标、计划或子代理列表。

## 9. 快速模型选择层

点击输入器中的模型标签后，首先出现轻量快速选择层，而不是直接进入复杂表单。

结构：

- 背景内容和键盘被明显模糊/降权。
- 居中显示当前“模型名 + 推理强度 + chevron”。
- 下方是宽胶囊选择轨道。
- 轨道内用若干圆点表达离散推理强度档位。
- 当前档位使用黑色填充区域和白色大圆点，其余档位为浅灰圆点。
- 点击某个档位立即更新强度。
- 点击模型标题或 chevron 进入高级配置。

无障碍要求：圆点不能是唯一信息。VoiceOver 必须读出档位名称，且应提供可访问的 Picker/Stepper 语义。

## 10. 高级模型配置 Sheet

高级配置以底部 sheet 进入：

- 背景会话内容保留可感知，但被 blur/scrim 降权。
- Sheet 顶部有短拖动指示条。
- 顶部标题为“Advanced”并带 disclosure chevron。
- 容器使用大圆角材质，顶部圆角约 32–40 pt。

表单字段必须来自 DSH 已加载的 provider/model catalog：

```text
Provider         当前提供商（有多个可选时）
Model            当前模型
Thinking effort  当前模型实际支持的推理强度
```

- 每个分组使用柔和灰色圆角表面。
- 行高约 56–64 pt。
- 左侧为字段名，右侧为当前值和上下选择指示。
- 同组内使用极细分隔线。
- 不为每一行增加独立卡片。
- 支持下拉菜单、Picker 或后续详情页，但交互必须一致。
- 截图中的 `Speed` 在 DSH 当前配置中没有对应字段，禁止加入。
- 推理强度档位必须来自所选模型返回的 `efforts`，不能固定画 5 个虚构档位。

## 11. 会话更多菜单

更多菜单锚定在右上角操作按钮附近，以系统 popover/menu 呈现。

仅允许呈现已经接通的会话动作。当前可用结构为：

```text
会话标题（弱标题，不可点击）
Rename
Fork
Export
------------------------------
Archive             destructive
```

视觉和行为：

- 宽度约 260–300 pt，使用系统材质和 28–36 pt 大圆角。
- 每行高度约 48–56 pt。
- 图标统一使用线性 SF Symbols。
- Archive 使用系统红色，但其他行保持 primary。
- 分隔线只用于划分管理操作与内容入口。
- 菜单外区域变暗但仍可辨认，点击外部关闭。

DSH 映射：

- Rename：映射 `AppModel.renameSession`。
- Fork：映射 `AppModel.forkSession`。
- Export：映射现有 session export 能力；未接到菜单前可暂不显示。
- Archive：映射 `AppModel.archiveSession`。
- Pin、Changes、Files 尚无完整的当前客户端能力，不进入菜单要求。读取与 diff 继续在真实工具活动中展示。

## 12. 动效和层级

- 浮层进入使用系统 sheet/menu 动效，时长约 250–400 ms。
- 状态胶囊的数据变化使用轻微淡入淡出，不进行大幅跳动。
- 停止/发送按钮状态切换可使用 150–220 ms 的图标替换。
- 模型强度轨道的黑色选区应连续移动，并尊重 Reduce Motion。
- 打开浮层时，背景 blur 与 scrim 同步渐变。
- 不允许多个 modal、popover 和键盘同时争夺焦点；打开高级 sheet 前先关闭快速选择层。

## 13. 对当前工程的映射

- `SessionDetailView`：顶部栏、正文、状态胶囊和运行状态编排。
- `MessageBubble` / `ActivityGroupCard`：用户气泡、助手正文、统一活动折叠。
- `ComposerView`：大圆角输入器和底部操作栏。
- `ModelPickerSheet`：快速强度选择与高级设置入口。
- `GoalCard`：可压缩为输入器上方目标胶囊，详细内容按需展开；内容仅取自 `GoalState`。
- `WorkSheet`：承载当前已有的任务和子代理详情，不凭空增加文件聚合。
- 会话管理动作：Rename/Fork/Archive；Export 仅在真实接线后显示。

## 14. DSH 能力映射表

| 参考界面概念 | DSH 真实来源 | 显示条件 | 当前结论 |
| --- | --- | --- | --- |
| 助手流式正文 | `assistant/chunk` / `text-delta`、`ChatItem.isStreaming` | 收到文本 chunk | 实现并保留 |
| 运行/停止 | `turn/start`、`turn/end`、`SessionModel.isRunning` | turn 运行中 | 实现并保留 |
| 思考过程 | `reasoning-delta` / assistant reasoning | 有非空 reasoning | 统一折叠 |
| 工具调用 | `tool/call`、`tool/result` | 有真实工具事件 | 统一折叠 |
| 计划状态 | `SessionModel.planActive` | 后端计划状态为真 | 可显示，不猜测文案 |
| 目标阶段 | `SessionModel.goal` | goal 存在 | 显示真实 phase/rounds |
| 子代理 | `SessionModel.subagents`、jobs | 列表非空 | 显示真实数量和状态 |
| 模型/强度 | `modelGroups`、`currentModelName`、`currentEffort` | catalog 加载成功 | 只列返回值 |
| 权限 | permission preset | 有当前 preset | 只读/工作区可写/完全访问 |
| 单次读取/diff | `ReadCard`、`DiffCard` | 工具事件包含该数据 | 使用专用卡片 |
| Steering 状态文字 | 无持久状态字段 | — | 不实现；steer 消息照常显示 |
| 目标用时 | 无可靠 startedAt | — | 不显示 |
| 文件与增删行总计 | 无会话级聚合状态 | — | 不显示 |
| Speed | 无配置字段 | — | 不实现 |
| Pin | 无会话置顶 RPC | — | 不实现 |

## 15. 验收清单

- 顶部栏是“左圆形按钮 + 双行标题 + 右操作胶囊”，不会遮挡正文。
- 助手正文直接排版，用户消息使用右对齐高对比气泡。
- reasoning、工具和子代理活动默认弱化且可展开。
- 输入器视觉上是一个整体，操作栏与文本区不重叠。
- 运行中只显示当前真实存在的目标、进度和子代理状态，不会挤占正文。
- 模型选择先轻量调整强度，再进入高级设置。
- 高级设置和更多菜单使用系统材质、正确圆角和分组。
- 破坏性动作只有 Archive 等明确操作使用红色。
- 浅色和深色模式均可读；放大字体后控件不重叠。
- 所有图标按钮、状态胶囊和强度档位均可被 VoiceOver 理解和操作。
- 页面不存在无法追溯到 DSH 状态/RPC 的假按钮、假数字或假阶段。
